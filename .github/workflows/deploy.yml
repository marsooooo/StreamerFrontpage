name: Deploy StreamerFrontpage (ON HOLD)

on:
  push:
    branches:
      - main

jobs:
  build-backend:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '24'

  #     - name: Install dependencies
  #       run: npm install

  #     - name: Build backend
  #       run: npm run server

  # build-frontend:
  #   needs: build-backend
  #   runs-on: ubuntu-latest
  #   steps:
  #     - uses: actions/checkout@v4

  #     - name: Setup Node.js
  #       uses: actions/setup-node@v4
  #       with:
  #         node-version: '24'

  #     - name: Install dependencies
  #       run: npm install

  #     - name: Build frontend
  #       run: npm run build:frontend

  # deploy-backend:
  #   needs: build-frontend
  #   runs-on: ubuntu-latest
  #   steps:
  #     - uses: actions/checkout@v4

  #     - name: Deploy backend via rsync
  #       env:
  #         DEPLOY_KEY: ${{ secrets.DEPLOY_SSH_KEY }}
  #         DEPLOY_HOST: ${{ secrets.DEPLOY_HOST }}
  #         DEPLOY_USER: ${{ secrets.DEPLOY_USER }}
  #       run: |
  #         mkdir -p ~/.ssh
  #         echo "$DEPLOY_KEY" > ~/.ssh/deploy_key
  #         chmod 600 ~/.ssh/deploy_key
  #         ssh-keyscan -H $DEPLOY_HOST >> ~/.ssh/known_hosts
  #         rsync -avz --delete -e "ssh -i ~/.ssh/deploy_key" . $DEPLOY_USER@$DEPLOY_HOST:/var/www/html/StreamerFrontpage/backend --exclude node_modules --exclude .git --exclude dist

  # deploy-frontend:
  #   needs: deploy-backend
  #   runs-on: ubuntu-latest
  #   steps:
  #     - uses: actions/checkout@v4

  #     - name: Setup Node.js
  #       uses: actions/setup-node@v4
  #       with:
  #         node-version: '18'

  #     - name: Install and build frontend
  #       run: |
  #         npm install
  #         npm run build:frontend

  #     - name: Deploy frontend via rsync
  #       env:
  #         DEPLOY_KEY: ${{ secrets.DEPLOY_SSH_KEY }}
  #         DEPLOY_HOST: ${{ secrets.DEPLOY_HOST }}
  #         DEPLOY_USER: ${{ secrets.DEPLOY_USER }}
  #       run: |
  #         mkdir -p ~/.ssh
  #         echo "$DEPLOY_KEY" > ~/.ssh/deploy_key
  #         chmod 600 ~/.ssh/deploy_key
  #         ssh-keyscan -H $DEPLOY_HOST >> ~/.ssh/known_hosts
  #         rsync -avz --delete -e "ssh -i ~/.ssh/deploy_key" dist/ $DEPLOY_USER@$DEPLOY_HOST:/var/www/html/StreamerFrontpage/dist

  # restart-server:
  #   needs: deploy-frontend
  #   runs-on: ubuntu-latest
  #   steps:
  #     - name: Restart PM2 server
  #       env:
  #         DEPLOY_KEY: ${{ secrets.DEPLOY_SSH_KEY }}
  #         DEPLOY_HOST: ${{ secrets.DEPLOY_HOST }}
  #         DEPLOY_USER: ${{ secrets.DEPLOY_USER }}
  #         DEPLOY_PM2_PROGRAM: ${{ secrets.DEPLOY_PM2_PROGRAM }}
  #       run: |
  #         mkdir -p ~/.ssh
  #         echo "$DEPLOY_KEY" > ~/.ssh/deploy_key
  #         chmod 600 ~/.ssh/deploy_key
  #         ssh-keyscan -H $DEPLOY_HOST >> ~/.ssh/known_hosts
  #         ssh -i ~/.ssh/deploy_key $DEPLOY_USER@$DEPLOY_HOST "pm2 restart $DEPLOY_PM2_PROGRAM"
